# GitHub Personal Access Tokens (comma-separated for rotation)
# Create at: https://github.com/settings/tokens
# Required scope: public_repo (read-only)
GH_TOKENS=ghp_xxxxxxxxxxxx,ghp_yyyyyyyyyyyy

# Server port (Docker)
PORT=3000

# Database path
DB_PATH=./data/github-stars.db
